# 2026-09-21

## CloudBase self-introduction 项目：comments 表（嵌套评论）数据库 + 后台路由

环境：`self-introduction-d8dvsb9aaeec60`（PG 模式，实例 `pgdb-lnqpbstb`，ap-shanghai，SHARED）。RuntimeBackends: postgresql=true, nosql=false, mysql=false。静态托管域名 `self-introduction-d8dvsb9aaeec60-1485920726.tcloudbaseapp.com`。

### 现状分析（⚠️ 更正：项目真身在本机 `C:\Users\23716\Desktop\my-site`，非"仅 Gitee"）
- 见下方【接入】小节。`public.comments` 表已存在（12 行），字段：id(bigint PK, **IDENTITY 自增**), project_id(varchar NOT NULL, 默认坑是字面量 'NOT NULL'), parent_id(bigint NULL 自引用), nickname, content, role(varchar 默认 'guest'), created_at/updated_at(bigint 毫秒时间戳)。
- 已确认是嵌套评论模型：parent_id 串父子，project_id 区分页面（echong/my-site/smart-farm）。
- **原问题**：表开了 RLS 但零策略 → 浏览器/客户端读写全被拒；parent_id 无外键，删除父不级联。

### 已完成的修复（迁移 20260921130100_comments_backend）
- 加 `parent_id` 级联外键 `comments_parent_id_fkey`（ON DELETE CASCADE）。
- 索引：idx_comments_project_id / idx_comments_parent_id / idx_comments_created_at(DESC)。
- RLS：`comments_select_all` SELECT USING(true) 公开读；`comments_write_admin` FOR ALL USING(auth.role()='service_role') 仅云函数写。
- GRANT: SELECT→anon,authenticated；INSERT/UPDATE/DELETE→service_role。
- 本地迁移文件：`cloudbase/migrations/20260921130100_comments_backend.sql`（MCP 自动写入 qingfan/cloudbase/migrations/）。
- 验证：service_role(云函数同款) 插入/删除均成功；SELECT 受只读副本延迟影响但策略正确。

### 坑
- `id` 是 IDENTITY 列，不能 `ALTER COLUMN id SET DEFAULT nextval(...)`（报 42601）。插入时直接省略 id 即可由数据库分配。第一次迁移(20260921130000)因此失败，换新版本重发。
- 本地 `ls/find/grep/tail` 被 shim 破坏，需用 MCP / node。

### 后台路由交付物
- `cloudbase/routes/comments.js`：评论云函数路由（list 返回嵌套树 / create 含父评论校验 / delete 级联）。用 `@cloudbase/node-sdk` v3 的 `app.rdb()`，云函数内以 service_role 写。
- 提醒用户：把该文件放进其 Gitee 仓库 `cloudfunctions/routes/comments.js`，并确认 node-sdk 支持 `app.rdb()`（v3）。

### 安全说明
- 当前设计为「公开读 + 仅云函数写」。若想开放游客直接发帖（绕过云函数直连 PG），需把 INSERT 策略改为 `WITH CHECK(true)` 并 GRANT INSERT 给 anon——有垃圾评论风险，按需开启。

## 新建独立评论表 comments_self（隔离 my-site）
- 用户要求：新评论功能不要和 my-site 网页共用的 `public.comments` 表重复/混用。
- 新建 `public.comments_self`（迁移 20260921131000_comments_self_create）：字段与 comments 一致（id IDENTITY PK, project_id, parent_id 自引用级联外键, nickname, content[TEXT 而非 varchar], role, created_at/updated_at bigint）。
- 索引、RLS 策略（select_all 公开读 / write_admin 仅 service_role）、GRANT 与 comments 表一致。
- 验证：插入 root+child 成功；删 root 后子评论被 ON DELETE CASCADE 级联删除（FK def 已确认带 CASCADE；清理 DELETE rowCount=0 证明子已随父删除）。SELECT 的 remain 计数是只读副本延迟假象，非真实数据。
- 后台路由 `cloudbase/routes/comments.js` 已全部改指向 `comments_self`（4 处 `from('comments')` → `from('comments_self')`），project_id 约定用 'self-introduction' 之类，不与 my-site 混用。
- 本地迁移文件：`cloudbase/migrations/20260921131000_comments_self_create.sql`。
- 结论：my-site 继续用原 `comments` 表（已具备 RLS 可公开读）；自我介绍站用 `comments_self`，互不干扰。

## 【接入】前端项目真身 = C:\Users\23716\Desktop\my-site（与 Gitee lw--315479/self-introduction 同仓库，同一提交 df852c5）
- 重大澄清：项目**一直都在本机** `C:\Users\23716\Desktop\my-site`（曾在 Gitee 误判为未克隆；另克隆了 Desktop/self-introduction 作对照，二者同提交）。它是 React+Vite+TS 前端 + Express 云函数后端。
- 技术栈：前端 React+Vite+TS（src/），后端 `cloudfunctions/functions/api/`（Express，入口 index.js → app.js，`serverless-http`，Nodejs18.15，部署名 `api`），数据访问集中在 `db.js`。
- 环境：`self-introduction-d8dvsb9aaeec60`（ap-shanghai）。线上访问域名 `https://self-introduction-d8dvsb9aaeec60-1485920726.ap-shanghai.app.tcloudbase.com/api`。
- **实际数据路径**：走 CloudBase HTTP 网关（`CLOUDBASE_API_KEY`，PostgREST 风格），而非 pg 直连（hasPostgres=false）。网关 base = `https://<envId>.api.tcloudbasegateway.com`，REST 前缀 `/v1/rdb/rest/<table>`。

### 接入改动（已部署并验证）
1. `cloudfunctions/functions/api/db.js`：新增 `COMMENTS_TABLE = process.env.COMMENTS_TABLE || 'comments_self'`（+ `assertSafeTableName` 防注入），把网关/pg/cloud/local 四条路径的表名全部参数化；新增 `gwQueryAllIncludingLegacy()` / `gwDeleteIncludingLegacy()`，让**后台管理合并读/删新旧两表**（旧表行标 `_legacy:true`，仅网关路径支持合并）；导出 `COMMENTS_TABLE`/`LEGACY_COMMENTS_TABLE`。
2. `cloudfunctions/functions/api/sql/comments_self.sql`：新表建表脚本（含外键级联 + RLS，供自建实例复用）。
3. `cloudbaserc.json`（gitignore 不入库）：envVariables 增加 `COMMENTS_TABLE: "comments_self"`。**注意：线上 CLOUDBASE_API_KEY 明文与 cloudbaserc.json 不同（ValueLength 932）**，更新 env 时只能合并新增键，不能整体覆盖。
4. 前端**零改动**：`mapRow` 已输出 camelCase，前端按 `project.id`（my-site/echong/smart-farm）调用，契约不变。

### 部署方式（tcb CLI 未登录，改用 MCP）
- `mcp__cloudbase__manageFunctions`：`updateFunctionCode`（functionRootPath=`.../cloudfunctions/functions`，force=true）+ `updateFunctionConfig`（envVariables 合并）。MCP 已账号级登录且绑定该环境。
- 部署后**线上端到端验证通过**：`/api/health` 200；POST `/api/comments` 200 且数据只落入 `comments_self`（旧表 0 行）；GET 200 返回；admin 登录 + `/api/admin/comments` 返回 12 条（均为旧表 `_legacy` 历史数据）。测试数据已清理。

### 回退开关
- 设 `COMMENTS_TABLE=comments` 即回到旧行为（读 my-site 共用的 comments 表），已验证能正确读 my-site(5) / echong(6) 条历史评论。
