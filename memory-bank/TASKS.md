# 青番 · TASKS

> 规则：每次只做一个编号任务；完成后同步 `PROGRESS.md`。

## A. 已完成

- [x] A1 工程脚手架：`Index` 启动分发、`main_pages.json` 注册、主题令牌与 4 套主题
- [x] A2 引导 `Onboarding`：4 页 Swiper 轮播 + 跳过/下一步 + 回放
- [x] A3 登录 `Login`：手机号+密码 / 注册（用户名/手机号/密码/确认密码/验证码）
- [x] A4 今日 `Home`：概览（带分隔线）、今日挑战（进度+调整）、AI 卡、待办列表
- [x] A5 待办交互：添加（时长预设+自定义）、完成、**长按拖动排序**、**右滑删除（二次确认）**
- [x] A6 专注 `Focus`：圆环 Canvas 倒计时、时长切换、**开始/暂停**、背景（天空/晨雾/夕阳，显示颜色）、白噪音、提前退出（原因）
- [x] A7 完成 `Complete`：种树庆祝 + 经验奖励（支持自定义标题）
- [x] A8 成长体系：签到（0 点刷新）、连续天数（签到且专注≥10min）、森林（每 10 番茄 1 棵树）、徽章、复活
- [x] A9 统计 `Stats`：数据卡 + AI 洞察 + 时间轴
- [x] A10 我的 `Me`：资料/经验/签到/森林/打卡/徽章/主题/小组件/新手指引/设置/退出
- [x] A11 子页：`EditProfile` / `Badges` / `Notify` / `ThemePicker` / `WidgetPicker` / `AIAssistant` / `AIChat`
- [x] A12 桌面小组件：`QfWidgetAbility` + `WidgetCard`（2×2/2×4/4×4）
- [x] A13 新手引导：首页高亮目标区域 + 气泡箭头（可回放）
- [x] A14 访客模式：游客可用待办+计时；AI/统计/自习室需登录
- [x] A15 账号按手机号隔离存储；退出登录保留账号数据；登录后重新加载

## B. 云端（三端互通）

- [x] B1 接入 CloudBase 云函数 `studyRoomFunctions`（env `cloud1-d5g8q89yd66340db4`），新增 `CloudFnService`
- [x] B2 自习室房间：创建 / 我的房间 / 按房间号加入 / 退出 / 解散（`CloudRoomService`）
- [x] B3 房间留言 + 回复（`CloudCommentService`，按 `parent_id` 组树展示）
- [x] B4 账号打通：登录/注册改走 `auth.login` / `auth.register`（与 Web/小程序同一套账号）
- [x] B5 房间内计时：`Focus` 房间模式（标题=房间名、完成后询问是否继续）；房间成员弹窗
- [x] B6 房间成员排行展示「专注时长」（本人本机累计；上报接口 `focus.record` 已预留）

## C. Web / 小程序端（功能与鸿蒙一致）

- [x] C1 引导 / 登录注册（手机号+密码，与鸿蒙同一套账号）
- [x] C2 今日待办（概览 / 挑战 / AI 建议 / 增删改 / 拖动排序 / 删除）
- [x] C3 专注计时（圆环 / 时长预设+自定义 / 开始暂停 / 背景三色 / 白噪音 / 提前退出原因 / 完成）
- [x] C4 成长体系（经验 / 等级 / 签到 / 连续 / 森林 / 徽章）
- [x] C5 统计（日 / 周 / 月、数据卡、AI 洞察、时间轴）
- [x] C6 自习室（房间增删改、按房间号加入/退出、留言+回复、房间内计时、成员排行）
- [x] C7 外观主题（4 套即时切换）与桌面小组件（等价展示）
- [x] C8 AI 助手（对话 + 建议）
- [x] C9 小程序：与 Web 同构（`wx.cloud.callFunction`，OPENID 身份）

## D. 待办 / 后续

- [ ] C1 【需后端】云函数加 `focus.record`，并在 `room.get` 成员返回 `nickname` / `focus_minutes`；`room_members_self` 加列
- [ ] C2 【需后端】房间成员真实昵称（当前非本人显示「用户 …末6位」）
- [ ] C3 真实短信验证码 / 真实微信登录（当前为演示）
- [ ] C4 专注统计上云（当前为本地按账号隔离）
- [ ] C5 删除无入口的 `Rank` 页面（或改从「我的」进入）
- [ ] C6 云函数 `listMine` 的 `member_count` 修正（当前恒为 1，App 已用 `room.get` 兜底）
